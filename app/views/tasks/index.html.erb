<h1>Tasks 😝</h1>
<ul>
  <% @tasks.each do |task| %>
    <li> <%= task.completed %> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<h4> <%= link_to "Create a new task", task_new_path %> </h4>